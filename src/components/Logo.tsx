import { Box, Image } from "@chakra-ui/react";

const Logo = () => {
  return (
    <Box display="flex" justifyContent="center">
      <Image src="/logo.svg" />
    </Box>
  );
};

export default Logo;
